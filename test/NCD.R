

load.NCData <- function(outputFile) {
  
  
  df <- data.frame(indicator="indicator NCD",
                   country="Americas",
                   year="2016",
                   value=1,
                   sourceOrg="NCD",
                   sourceYear=2015,
                   isRegion=1)
  
  return(df)
}

